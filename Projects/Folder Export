from os import listdir #play with the underlying OS you're working with it
import shutil

source_path = ''
source_files = listdir(source_path)
destination_path = ''

for file in source_files:
    if file.endswith('.csv'):
        shutil.move(os.path.join(sourcepath,file), os.path.join(destinationpath)
